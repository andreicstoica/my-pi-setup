# Dependencies
node_modules/

# Downloaded/generated executables
bin/

# Pi runtime and private state
sessions/
# Memory content is personal notes about the user's work; this repo is public.
# Root-anchored: the extension at extensions/memory/ IS committed, its data is not.
/memory/
/workflows/
auth.json
settings.json
trust.json
models.json
models-store.json
extensions/summaries/config.private.json
extensions/summaries/config.private.json.*.tmp

# Environment variables and secrets
.env
.env.*
!.env.example
*.pem
*.key

# Logs and temporary files
*.log
*.tmp
*.swp
.DS_Store

# Local editor settings
.vscode/
.idea/
